% ============================================================
% Verification Collapse: Self-Improving LLMs Lose Calibration
% NeurIPS 2026 Submission
% Deadline: abstract May 5 / paper May 7, 2026 (Europe/Copenhagen)
% ============================================================

\documentclass{article}
% NOTE: swap the line below for \usepackage[preprint]{neurips_2026} once you
% have the NeurIPS 2026 style file (download from the submission site).
\usepackage[margin=1in]{geometry}  % plain fallback for local preview
\usepackage[utf8]{inputenc}
\usepackage{amsmath, amssymb, amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\graphicspath{{figures/}}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{microtype}
\usepackage{natbib}

% ---- convenience macros ----
\newcommand{\gap}{\mathcal{G}}          % verification gap
\newcommand{\self}{s_{\text{self}}}     % self-score
\newcommand{\gt}{s_{\text{gt}}}         % ground-truth score
\newcommand{\todo}[1]{\textcolor{red}{[TODO: #1]}}

% ============================================================
\title{Verification Collapse in Iterative Self-Improving Language Models}

\author{
  \todo{Author list}\\
  \todo{Institution}
}

\begin{document}
\maketitle

% ============================================================
\begin{abstract}
% HOOK: ~150 words. State the problem, the finding, and why it matters.
% Draft tomorrow — data is in hand.
%
% Skeleton:
%   Sentence 1: Self-improvement loops are promising but assume the model
%               can reliably judge its own outputs.
%   Sentence 2: We show this assumption fails: the verification gap grows.
%   Sentence 3: We call this verification collapse and quantify it.
%   Sentence 4: We show adversarial injection bounds the gap.
%   Sentence 5: Implications for RLHF, AZR, STaR-like methods.
\todo{Write abstract last.}
\end{abstract}

% ============================================================
\section{Introduction}
\label{sec:intro}

% --- WRITING HOOK (expand tomorrow) ---
%
% Opening image: a student who grades their own exams gets an A every time,
% while their actual test scores stay flat. That is verification collapse.
%
% Paragraph 1 — motivation:
%   Iterative self-improvement (STaR, AZR, RLHF) relies on the model's
%   ability to evaluate its own outputs. If the judge and the generator are
%   the same model, and the model is fine-tuned on examples it judges
%   correct, any systematic bias in self-evaluation compounds over iterations.
%
% Paragraph 2 — the gap and why it matters:
%   Define the verification gap G_t = self_score_t - gt_score_t.
%   Claim: G_t grows monotonically with t in the absence of external signal.
%   Consequence: the model becomes increasingly confident in wrong answers,
%   silently degrading the quality of the training signal it generates for
%   itself. This is distinct from reward hacking (external reward model) —
%   it emerges endogenously in self-supervised loops.
%
% Paragraph 3 — what we do:
%   We run a controlled experiment on MATH Level 3–5 (250 problems) with
%   Qwen2.5-7B-Instruct over 20 self-improvement iterations. We measure G_t
%   at each iteration. We observe G grows from 0.10 to 0.32 while gt_val
%   stays flat. We then show that injecting hard negatives — examples the
%   model was confidently wrong about — into the fine-tuning batch every
%   3 iterations bounds G_t at ~0.17, a 47\% reduction.
%
% Paragraph 4 — contributions:
%   (i)  We define verification collapse and provide the first controlled
%        quantitative measurement of the phenomenon.
%   (ii) We introduce adversarial hard-negative injection as a lightweight
%        mitigation requiring no external oracle.
%   (iii) We situate the result relative to AZR, STaR, and RM overoptimization
%        literature, arguing verification collapse is a distinct failure mode.

The prospect of language models that improve themselves — iteratively
refining their outputs without human annotation at every step — has reshaped
the frontier of AI training.
Methods ranging from STaR \citep{zelikman2022star} and Self-Refine
\citep{madaan2023selfrefine} to Constitutional AI \citep{bai2022constitutional}
and Absolute Zero Reasoner \citep{deepseek2025azr} share a common design:
the model generates a candidate answer, evaluates it, and uses that evaluation
to select or improve training examples.
When this loop is run iteratively with fine-tuning between rounds, it becomes
a self-reinforcing engine of improvement that scales without any external
oracle.
The appeal is straightforward: human-labelled data is expensive, slow, and
finite; a model that can judge and teach itself is, in principle, unbounded.

Every such loop rests on a single structural assumption: that the model's
self-evaluation is reliable enough to distinguish correct from incorrect
outputs.
In STaR, the model's self-judgment selects which chains-of-thought enter the
training batch.
In Constitutional AI, self-critique drives the revision.
In Self-Rewarding Language Models \citep{yuan2024self}, the model assigns its
own reward signal at every iteration.
In Absolute Zero Reasoner, the model verifies solutions it generates to its
own proposed tasks.
Across all these architectures, self-verification is not a peripheral
component — it is the mechanism that converts generation into a training
signal.
If self-judgment is systematically miscalibrated, the loop does not merely
slow down; it trains the model on increasingly wrong evidence, with increasing
confidence, silently degrading the quality of every subsequent iteration.

There is growing evidence that this assumption is fragile.
\citet{huang2024cannot} demonstrate that LLMs cannot reliably self-correct
reasoning without access to external feedback — intrinsic self-critique
degrades performance on tasks where ground truth is unavailable.
Concurrent work documents growing calibration error across iterative
self-improvement rounds \citep{li2025beyondaccuracy,epicar2026}, and
\citet{rlsr2025} observe self-reward diverging from formal reward under RL
pressure.
Yet a precise, dynamic characterisation of this failure mode — how the gap
between a model's self-assigned score and external accuracy evolves over the
course of a full self-improvement loop, and what causes it — has been absent
from the literature.
Prior work measures calibration using aggregate, post-hoc statistics (Expected
Calibration Error, AUROC) over short horizons of three to five rounds; no
study has tracked the raw per-iteration drift over a full 20-round loop,
isolated self-scoring as its causal driver, or characterised how collapse
severity varies across model families.

We close this gap.
We define the \emph{verification gap} at iteration $t$ as
$\Delta_t = \bar{s}_t^{\text{self}} - \bar{s}_t^{\text{ext}}$,
where $\bar{s}_t^{\text{self}}$ is the model's mean self-assigned correctness
score and $\bar{s}_t^{\text{ext}}$ is the mean ground-truth accuracy on the
same training examples.
We run 20 iterations of self-improvement on the MATH benchmark (Level 3--5)
with Qwen2.5-7B-Instruct \citep{qwen2025}, observing $\Delta_t$ grow from
$0.105$ at iteration~1 to $0.320$ at iteration~20 — a three-fold increase —
while ground-truth validation accuracy remains flat.
We replicate the effect across two additional model families, uncovering a
\emph{two-regime} structure: capable models drift gradually as self-scores
inflate beyond genuine accuracy; weaker models collapse immediately from the
first iteration, with the loop reinforcing the collapsed state
(Mistral-7B: $\Delta_1 = 0.580$, $\Delta_{20} = 0.850$).
We isolate self-scoring as the causal driver via a GT-anchored control — the
same loop but trained on ground-truth-correct examples rather than
self-judged-correct ones — where the gap \emph{decreases} over 20 iterations
while genuine accuracy improves by $78\%$.
Finally, we propose \emph{adversarial hard-negative injection}: periodically
replacing a fraction of the fine-tuning batch with high-confidence errors
trained on gold solutions, reducing the steady-state gap by $47\%$ without
any external oracle.

Our contributions are threefold.
\textbf{(i)}~We provide the first controlled, longitudinal measurement of
\emph{verification collapse} — a failure mode distinct from reward model
overoptimization \citep{gao2023scaling} and from the filtering-utility gap of
\citet{song2025mindthegap} — tracking $\Delta_t$ as a live per-iteration
scalar over 20 rounds across three model families.
\textbf{(ii)}~We establish a causal link between self-scoring and collapse
through a GT-anchored ablation, ruling out iterative fine-tuning per se as
the mechanism.
\textbf{(iii)}~We propose adversarial hard-negative injection as a
lightweight, data-side mitigation requiring no objective modification, no
external judge, and no architectural change, making it applicable to any
self-improving system.

% ============================================================
\section{Background and Related Work}
\label{sec:related}

\paragraph{Iterative self-improvement.}
Iterative self-improvement methods span a spectrum of how much external signal they
admit into the training loop.
STaR \citep{zelikman2022star} anchors every iteration to ground truth: when the model
fails to produce a correct chain-of-thought, it is shown the reference answer and asked
to rationalize it, ensuring that external correctness never leaves the loop.
Collapse cannot emerge by design, because the training filter is ground truth, not
self-judgment — a fact we exploit in our GT-anchored control condition
(Section~\ref{sec:results}).
At the other extreme, Absolute Zero Reasoner \citep{deepseek2025azr} proposes fully
closed self-improvement in which the model proposes tasks, solves them, and verifies its
own solutions, implicitly assuming that self-verification remains calibrated across
training.
That assumption has not been tested empirically; the present work provides the test, and
it fails.
ReST \citep{gulcehre2023rest} and related RLHF variants interpose an external reward
model between generator and training signal, shifting the failure mode from endogenous
calibration drift to reward model overoptimization \citep{gao2023scaling} — a related
but distinct problem we address next.

\paragraph{Reward model overoptimization and endogenous collapse.}
\citet{gao2023scaling} establish that reward model scores diverge from
ground truth under KL-unconstrained optimization: as the policy moves far from the
initial distribution, an external RM's scores become increasingly detached from true
quality.
Their setting requires a separate RM distinct from the generator.
We show the same score-versus-reality divergence emerges \emph{endogenously} in closed
self-supervised loops — without any external reward model and without any explicit RL
pressure.
The self-verifier acts as an \emph{implicit, unconstrained reward model} across
iterations: each fine-tuning step is supervised by examples the model has awarded high
self-scores, and absent a KL term or external anchor, those scores inflate without bound.
Verification collapse is, in this framing, what KL-unconstrained reward hacking looks like
when the reward model is the policy itself.
Concurrent work by RLSR \citep{rlsr2025} documents precisely this divergence in the
GRPO/RL regime with explicit reward signal; our contribution is demonstrating that the
same collapse emerges without RL pressure — pure iterative SFT is sufficient.
The self-verifier's endogenous character makes the failure mode both harder to detect and
harder to escape, since there is no external component to freeze or replace.

\paragraph{Generation-verification gap.}
\citet{song2025mindthegap} define the generation-verification gap as
the accuracy improvement obtainable by filtering a model's own generations through
self-verification — formally, $\text{gap}(f,g) = J(f[w(\hat{u}_g)]) - J(f)$, where
$\hat{u}_g$ is the self-verifier and $w(\cdot)$ denotes the filtering operator.
Their central result is that this gap saturates to zero within two to three rounds of
iterative self-improvement, driven by diversity collapse in the generation distribution.
We study a complementary but distinct failure mode: not whether the verifier can
\emph{rank} generations correctly, but whether the model's self-assigned scores remain
\emph{calibrated to ground truth} as training progresses — a divergence that can grow
even as Song et al.'s gap collapses.
In Algorithm~1 of their work, self-verification scores function purely as a binary
filter (threshold $\tau$); the scalar score values are never recorded or tracked.
A model that grows monotonically overconfident across all 20 iterations would be
undetectable in their setup.
We measure exactly what their setup leaves unobserved: $\Delta_t = \bar{s}_t^{\text{self}} - \bar{s}_t^{\text{ext}}$.

\paragraph{Judge-generator co-degradation.}
\citet{yuan2024self} qualitatively observe that, in iterative self-reward
fine-tuning, the model's capacity as a judge degrades alongside its capacity as a
generator — a co-degradation they flag as a form of reward hacking and leave as an open
problem.
Their analysis spans three iterations and does not record $\Delta_t$.
We quantify this phenomenon in a controlled 20-iteration experiment, define $\Delta_t$
as the primary diagnostic metric, and show the gap grows monotonically in the
unmitigated baseline while adversarial injection bounds it without requiring any external
judge.

\paragraph{Concurrent calibration work.}
Three concurrent works document related calibration failures in iterative
self-improvement, each measuring a distinct quantity in a distinct regime.
\citet{li2025beyondaccuracy} measure Expected Calibration Error (ECE) —
an aggregate binned statistic over confidence deciles — on general QA (MMLU) with
Llama-2 and DeepSeek-R1-Distill over five rounds, proposing iterative temperature
scaling as a post-hoc recalibration fix applied between rounds.
EpiCaR \citep{epicar2026} extends the calibration audit to MATH, GSM8K, and code,
measuring ECE, AUROC, and Brier Score across six model sizes over three iterations, and
proposes a dual-task training objective that jointly reinforces correct reasoning and
explicit self-evaluation on incorrect outputs.
RLSR \citep{rlsr2025} studies the RL (GRPO) regime, observing that continuous joint
updates to generator and judge cause self-reward to diverge from formal reward on
synthetic puzzle tasks, and proposes freezing the judge by substituting an external
evaluator.
Our work differs from all three along three axes.
First, we track $\Delta_t$ as a \emph{raw per-iteration scalar} rather than an aggregate
post-hoc calibration metric; $\Delta_t$ is computable online without binning
infrastructure and reveals the monotonic growth trajectory that aggregate metrics
obscure.
Second, we extend the diagnostic to 20 iterations — the only study long enough to
confirm monotonic growth rather than a transient artifact.
Third, our mitigation, adversarial hard-negative injection, is data-side and in-loop:
it requires neither objective modification (contra EpiCaR) nor an external judge model
(contra RLSR), making it applicable to any self-improving system without architectural
changes.

% ============================================================
\section{Setup}
\label{sec:setup}

\subsection{Task and Data}

We evaluate on a 250-problem subset of the MATH benchmark
\citep{hendrycks2021math}, restricted to difficulty levels 3–5.
Problems are split 200/50 into train and validation sets (seed 42).
The dataset is fixed across all iterations; only the model changes.

\subsection{Model}

We use Qwen2.5-7B-Instruct \citep{qwen2025} in BF16 precision with
Flash Attention 2 \citep{dao2023flashattention2} and a QLoRA adapter
(rank $r=16$, $\alpha=32$, all-linear target modules, no DoRA).
No 4-bit quantization is applied; the model runs at full BF16 precision
on an NVIDIA H100 SXM (80 GB).

\subsection{The Iterative Self-Improvement Loop}

At each iteration $t = 1, \ldots, 20$:
\begin{enumerate}
  \item \textbf{Generate.} The model produces a solution for each training
        problem using greedy decoding ($T=0$, \texttt{max\_new\_tokens}=512).
  \item \textbf{Self-score.} The same model judges each solution as correct
        or incorrect (\texttt{yes}/\texttt{no}) without access to the
        reference answer. This yields self-score $\self^{(t)} \in \{0,1\}$
        per sample.
  \item \textbf{Filter and fine-tune.} The model is fine-tuned (one epoch,
        QLoRA, effective batch 32) on the subset of problems for which
        $\self^{(t)} > 0$, using the model's own completion as the
        training target.
  \item \textbf{Evaluate.} Ground-truth accuracy $\gt^{(t)}$ is measured on
        the held-out validation set by extracting the final numerical answer
        and comparing to the reference.
\end{enumerate}

\subsection{Measurements}

The \textbf{verification gap} at iteration $t$ is defined as:
\begin{equation}
  \gap_t = \overline{\self}^{(t)} - \overline{\gt}_{\text{train}}^{(t)}
  \label{eq:gap}
\end{equation}
where $\overline{\self}^{(t)}$ is the mean self-score on the training batch
and $\overline{\gt}_{\text{train}}^{(t)}$ is the mean ground-truth accuracy
on the same batch. A positive and growing $\gap_t$ indicates the model
increasingly overestimates its own correctness.

\subsection{Adversarial Hard-Negative Injection}

In the injection variant, every 3 iterations we replace 50\% of the
fine-tuning batch with \emph{hard negatives}: examples from a running bank
where $\self \geq 0.7$ but the extracted answer is incorrect. Hard negatives
are trained with the gold MATH solution as the target, providing a
recalibration signal grounded in external correctness.

% ============================================================
\section{Results}
\label{sec:results}

\subsection{Baseline: Verification Collapse}

% --- WRITING HOOK ---
%
% Key numbers to hit:
%   - G grows from 0.105 (iter 1) to 0.320 (iter 20) — 3× increase
%   - self_score climbs 0.465 → 0.770
%   - gt_val stays flat 0.38–0.46 (no trend)
%   - loss → near zero by iter 10 (memorization of wrong answers)
%   - hard_negs grow 25 → 67 (accumulating overconfident wrong answers)
%
% Framing: the model is not learning to solve math problems — it is learning
% to ratify its own wrong answers. The self-score inflation is not a
% measurement artifact; the model genuinely believes it is correct.

Table~\ref{tab:baseline} and Figure~\ref{fig:baseline-injection}(a) report
the full baseline trajectory.
The verification gap $\gap_t$ grows monotonically from $0.105$ at iteration~1
to $0.320$ at iteration~20 — a three-fold increase over the course of
self-improvement.
While $\bar{\self}$ climbs from $0.465$ to $0.770$, ground-truth val accuracy
$\bar{\gt}_\text{val}$ remains effectively flat across all 20 iterations
($0.400$ at iteration~1, $0.460$ at iteration~20), confirming that the
model's rising self-confidence reflects a distortion of the verification
signal rather than genuine problem-solving improvement.
The near-zero training loss by iteration~10 ($\mathcal{L}_{10} = 0.006$)
corroborates this interpretation: the model has memorised its own overconfident
completions rather than learned to solve the underlying problems.

\begin{table}[h]
\centering
\caption{Baseline run: verification collapse over 20 iterations.
$\overline{\self}$: mean self-score (train). $\overline{\gt}_\text{val}$:
ground-truth accuracy (val). $\gap$: verification gap (Eq.~\ref{eq:gap}).
$\mathcal{L}$: fine-tuning loss.}
\label{tab:baseline}
\small
\begin{tabular}{rccccr}
\toprule
Iter & $\overline{\self}$ & $\overline{\gt}_\text{val}$ & $\gap$ & $\mathcal{L}$ & Hard negs \\
\midrule
1  & 0.465 & 0.400 & 0.105 & 0.0624 & 25 \\
5  & 0.620 & 0.420 & 0.220 & 0.0184 & 47 \\
10 & 0.675 & 0.440 & 0.250 & 0.0056 & 52 \\
15 & 0.750 & 0.440 & 0.295 & 0.0033 & 62 \\
20 & 0.770 & 0.460 & 0.320 & 0.0010 & 67 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{fig1_baseline_injection.pdf}
  \caption{%
    \textbf{(a)} Score trajectories in the baseline run (Qwen2.5-7B, MATH L3--5, 20 iterations).
    Self-score $\bar{\self}$ climbs steadily while GT-train and GT-val remain flat,
    opening a verification gap $\Delta_{20}=0.32$ (shaded region).
    Near-zero training loss by iteration~10 confirms memorisation rather than learning.
    \textbf{(b)} Verification gap $\Delta_t$ for baseline vs.\ adversarial injection
    (every third iteration, 50\% hard negatives, dotted verticals).
    Injection produces a sawtooth trajectory; the steady-state gap at iteration~20
    is reduced by 47\% (0.320 $\to$ 0.170).}
  \label{fig:baseline-injection}
\end{figure}

\subsection{Injection: Bounding the Gap}

% --- WRITING HOOK ---
%
% Key numbers to hit:
%   - Steady-state gap (non-injection iters) stays at 0.065–0.19 vs 0.25–0.32 baseline
%   - At iter 20: injection gap 0.170 vs baseline 0.320 (−47%)
%   - gt_val trends upward in injection (reaches 0.50–0.52) vs flat baseline
%   - gt_score_train higher in injection non-inj iters (0.42–0.53 vs 0.36–0.45)
%   - Injection-iteration spikes are batch-composition artifacts (explain clearly)
%   - Hard negatives grow and then stabilise — the bank gets saturated
%
% Framing: injection does not eliminate the gap — it periodically resets
% the model's overconfidence. The steady state after each injection is
% consistently lower than the equivalent baseline iteration, and the model
% makes genuine accuracy gains (gt_val rising) rather than just suppressing
% self-score.

The injection run produces a sawtooth gap trajectory visible in
Figure~\ref{fig:baseline-injection}(b): the gap spikes on injection iterations
(batch-composition artifact — see Section~\ref{sec:setup}) and returns to a
lower steady state on subsequent iterations.
Excluding injection iterations, the steady-state gap ranges from $0.065$ to
$0.175$ across iterations~2--20, compared to $0.160$--$0.320$ over the same
window in the baseline — a consistent reduction at every comparable checkpoint.
At iteration~20 the gap stands at $0.170$ versus $0.320$ in the baseline,
a 47\% reduction (Table~\ref{tab:injection}, Figure~\ref{fig:baseline-injection}(b)).
Crucially, the mitigation is accompanied by genuine accuracy gains: $\bar{\gt}_\text{val}$
trends upward in the injection run, reaching $0.50$--$0.52$ in the final iterations,
versus a flat $0.44$--$0.46$ in the baseline.
The model is not merely suppressing self-score — the recalibration signal
from hard negatives also improves the quality of its generated solutions.

\begin{table}[h]
\centering
\caption{Injection run: steady-state gap (non-injection iterations) compared
to baseline. $\bigstar$ marks injection iterations (batch-composition spikes, excluded
from steady-state comparison).}
\label{tab:injection}
\small
\begin{tabular}{rccccc}
\toprule
Iter & $\overline{\self}$ & $\overline{\gt}_\text{val}$ & $\gap$ & $\mathcal{L}$ & Injected \\
\midrule
1  & 0.465 & 0.420 & 0.105 & 0.0624 &   \\
2  & 0.515 & 0.380 & 0.110 & 0.0368 &   \\
3  & 0.635 & 0.380 & 0.265 & 0.1915 & $\bigstar$ \\
4  & 0.510 & 0.400 & 0.085 & 0.0224 &   \\
5  & 0.510 & 0.400 & 0.090 & 0.0195 &   \\
6  & 0.630 & 0.460 & 0.325 & 0.2140 & $\bigstar$ \\
7  & 0.500 & 0.460 & 0.105 & 0.0163 &   \\
8  & 0.470 & 0.460 & 0.065 & 0.0192 &   \\
9  & 0.680 & 0.460 & 0.410 & 0.1948 & $\bigstar$ \\
10 & 0.565 & 0.440 & 0.120 & 0.0207 &   \\
15 & 0.800 & 0.460 & 0.410 & 0.1234 & $\bigstar$ \\
16 & 0.635 & 0.440 & 0.150 & 0.0080 &   \\
20 & 0.695 & 0.420 & 0.170 & 0.0120 &   \\
\bottomrule
\end{tabular}
\end{table}

\subsection{GT-Anchored Control: Isolating the Causal Driver}

To isolate self-scoring as the mechanism driving collapse — rather than
iterative fine-tuning per se — we run a third condition identical to the
baseline in all respects except the training filter.
Instead of fine-tuning on examples the model judges correct
($\self^{(t)} > 0$), we fine-tune on examples verified correct against the
reference answer ($\gt^{(t)} > 0$).
This is the STaR-style anchor \citep{zelikman2022star}: ground truth never
leaves the loop.

Table~\ref{tab:gtanchored} reports the full 20-iteration trajectory.
The verification gap starts at $\gap_1 = 0.190$ and ends at
$\gap_{20} = 0.122$ — a slight \emph{decrease}, bounded throughout in the
range $[0.083, 0.193]$.
This is categorically different from the baseline, where the gap grew
monotonically from $0.105$ to $0.320$.
The model and loop are identical; only the filter differs.
Self-scoring is the causal driver of collapse.

Notably, ground-truth training accuracy improves substantially under the
GT-anchored condition: $\overline{\gt}_{\text{train}}$ rises from $0.300$
at iteration~1 to $0.535$ at iteration~20 ($+78\%$), compared to a modest
$0.360 \to 0.450$ ($+25\%$) in the baseline.
The model genuinely learns to solve more problems when trained on correct
evidence rather than reinforcing overconfident wrong answers.
Hard-negative accumulation reverses accordingly: the bank shrinks from 79
to 62 high-confidence errors as actual accuracy improves — the opposite of
the baseline trend (25 $\to$ 67).

\begin{table}[h]
\centering
\caption{GT-anchored control: gap trajectory over 20 iterations.
Training filter is ground-truth correctness ($\gt^{(t)} > 0$) rather than
self-score. The gap is bounded throughout and ends lower than it started,
in contrast to the $3\times$ growth in the baseline.}
\label{tab:gtanchored}
\small
\begin{tabular}{rcccccr}
\toprule
Iter & $\overline{\self}$ & $\overline{\gt}_\text{train}$ & $\overline{\gt}_\text{val}$ & $\gap$ & $\mathcal{L}$ & Hard negs \\
\midrule
1  & 0.490 & 0.300 & 0.370 & 0.190 & 0.0672 & 79 \\
5  & 0.540 & 0.383 & 0.360 & 0.158 & 0.0178 & 68 \\
10 & 0.603 & 0.468 & 0.370 & 0.135 & 0.0101 & 61 \\
15 & 0.608 & 0.500 & 0.390 & 0.108 & 0.0074 & 54 \\
20 & 0.658 & 0.535 & 0.370 & 0.122 & 0.0023 & 62 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Cross-Model Generalizability: Three Collapse Regimes}

To test whether verification collapse is an artifact of Qwen2.5-7B-Instruct
specifically, we run the baseline self-score loop on two additional model
families: Mistral-7B-Instruct-v0.3 \citep{jiang2023mistral} and
Llama-3-8B-Instruct \citep{llama3team2024}, under identical conditions
(MATH Level 3--5, 400 train / 100 val, 20 iterations).
The results reveal that verification collapse is universal but
\emph{model-capability-dependent}, exhibiting three qualitatively distinct
trajectory regimes.

\textbf{Mistral-7B: immediate collapse.}
Table~\ref{tab:mistral} shows the most extreme case.
The gap begins at $\gap_1 = 0.580$ — already larger than Qwen's
\emph{final} gap of $0.320$ — and grows monotonically to $\gap_{20} = 0.850$.
Ground-truth training accuracy remains essentially zero throughout
($0.035$--$0.065$): Mistral-7B cannot solve MATH Level 3--5 problems, yet
its self-score climbs from $0.625$ to $0.900$.
By iteration~20 the model judges itself $90\%$ correct while achieving $5\%$
ground-truth accuracy — an 85 percentage-point gap.
Because the model's baseline capability on this domain is near zero, there is
no gradual drift phase: collapse is immediate, and the loop locks in and
amplifies the collapsed state.

\textbf{Llama-3-8B: transient collapse.}
Table~\ref{tab:llama3} shows a third, distinct pattern.
The gap starts small ($\gap_1 = 0.058$), grows steadily to a peak of
$\gap_{13} = 0.368$, then \emph{partially recovers}, falling to
$\gap_{20} = 0.148$.
Crucially, this recovery is not genuine recalibration: ground-truth training
accuracy remains flat throughout ($0.22$--$0.24$), and gt\_val does not
improve.
Instead, the LoRA adapter — after sufficiently many iterations — distorts the
model's instruction-following regime enough to degrade self-judgment itself,
causing self-scores to fall back toward the model's actual accuracy.
The recovery in the gap is driven by a \emph{collapse of overconfidence},
not by improved reasoning.
Hard negatives peak at 160 (iteration~13) and then shrink to 92 as the model
becomes less willing to produce high-confidence answers of any kind.

Taken together, the three regimes are ordered by the model's baseline
ground-truth accuracy on the domain:
capable models ($\sim$36\% base accuracy) show \emph{gradual monotonic}
collapse; moderately capable models ($\sim$23\%) show \emph{transient}
collapse with partial recovery driven by self-judgment degradation; and
near-incapable models ($\sim$5\%) show \emph{immediate} collapse with
monotonic amplification.
In all three cases, genuine accuracy (gt\_train, gt\_val) never improves under
the self-score loop, confirming that verification collapse is not regime-specific.

\begin{table}[h]
\centering
\caption{Mistral-7B-Instruct-v0.3 baseline. Immediate collapse from
iteration~1; gap grows monotonically to $0.850$ while ground-truth
accuracy remains at $\approx\!5\%$ throughout.}
\label{tab:mistral}
\small
\begin{tabular}{rcccccr}
\toprule
Iter & $\overline{\self}$ & $\overline{\gt}_\text{train}$ & $\overline{\gt}_\text{val}$ & $\gap$ & $\mathcal{L}$ & Hard negs \\
\midrule
1  & 0.625 & 0.045 & 0.080 & 0.580 & 0.3967 & 232 \\
5  & 0.618 & 0.063 & 0.090 & 0.555 & 0.0645 & 223 \\
10 & 0.773 & 0.053 & 0.120 & 0.720 & 0.0385 & 288 \\
15 & 0.878 & 0.050 & 0.050 & 0.828 & 0.0248 & 331 \\
20 & 0.900 & 0.050 & 0.050 & 0.850 & 0.0200 & 340 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\caption{Llama-3-8B-Instruct baseline. Transient collapse: gap peaks at
$0.368$ (iteration~13) then partially recovers to $0.148$ as LoRA distortion
degrades self-judgment itself. Ground-truth accuracy remains flat throughout.}
\label{tab:llama3}
\small
\begin{tabular}{rcccccr}
\toprule
Iter & $\overline{\self}$ & $\overline{\gt}_\text{train}$ & $\overline{\gt}_\text{val}$ & $\gap$ & $\mathcal{L}$ & Hard negs \\
\midrule
1  & 0.288 & 0.230 & 0.320 & 0.058 & 0.1280 & 66  \\
5  & 0.433 & 0.235 & 0.310 & 0.198 & 0.0241 & 106 \\
10 & 0.555 & 0.230 & 0.260 & 0.325 & 0.0093 & 142 \\
13 & 0.590 & 0.223 & 0.280 & 0.368 & 0.0079 & 160 \\
20 & 0.378 & 0.230 & 0.290 & 0.148 & 0.0187 & 92  \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.75\linewidth]{fig2_cross_model.pdf}
  \caption{Verification gap $\Delta_t$ across three model families and the
    GT-anchored control (Qwen2.5-7B).
    Three distinct collapse regimes emerge: gradual monotonic collapse (Qwen2.5-7B,
    $\Delta_{20}=0.32$), immediate lock-in (Mistral-7B, $\Delta_1=0.58$,
    $\Delta_{20}=0.85$), and transient peak-and-partial-recovery
    (Llama-3-8B, peak $\Delta_{13}=0.37$, $\Delta_{20}=0.15$).
    The GT-anchored condition (dot-dash) confirms that replacing the self-score
    training filter with ground truth eliminates collapse entirely.}
  \label{fig:cross-model}
\end{figure}

\subsection{Summary}

Table~\ref{tab:summary} compares all conditions.
Verification collapse is not a Qwen artifact: all three model families
exhibit it under the self-score loop, with collapse severity and trajectory
shape scaling with the model's baseline capability on the domain.
The causal variable is the self-score training filter: replacing it with
ground-truth verification (GT-anchored) eliminates collapse and produces
genuine accuracy gains, while adversarial injection reduces the Qwen
steady-state gap by 47\% without any external oracle.

\begin{table}[h]
\centering
\caption{All conditions. For Llama-3, peak $\gap$ (iteration~13) is shown
alongside $\gap_{20}$ to capture the transient trajectory.}
\label{tab:summary}
\small
\begin{tabular}{llcccc}
\toprule
Model & Condition & $\gap_1$ & Peak $\gap$ & $\gap_{20}$ & $\overline{\gt}_\text{train,20}$ \\
\midrule
Qwen2.5-7B  & Baseline (self-score)         & 0.105 & 0.320        & 0.320 & 0.450 \\
Qwen2.5-7B  & Injection ($N{=}3$, 50\%)     & 0.105 & 0.475        & 0.170 & 0.525 \\
Qwen2.5-7B  & GT-anchored                   & 0.190 & 0.193        & 0.122 & 0.535 \\
\midrule
Mistral-7B  & Baseline (self-score)         & 0.580 & 0.853        & 0.850 & 0.050 \\
Llama-3-8B  & Baseline (self-score)         & 0.058 & 0.368 (i13)  & 0.148 & 0.230 \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================
\section{Discussion}
\label{sec:discussion}

% --- WRITING HOOK ---
%
% Three paragraphs:
%
% 1. Why does collapse happen?
%    The model fine-tunes on its own completions for problems it thinks it
%    solved. Once the model is systematically overconfident (which happens
%    fast — by iter 3–4 the gap is already 0.20+), it trains on wrong
%    completions. The LoRA adapter amplifies confidence in those wrong
%    completions. The self-score then reflects LoRA-distorted priors, not
%    correctness. Loss→0 confirms memorization.
%
% 2. Why does injection help?
%    Hard negatives provide the only ground-truth-anchored signal in the loop.
%    By forcing the model to see gold solutions for problems it was confidently
%    wrong about, the injection step partially overwrites the overconfident
%    LoRA weights. The model's self-score drops back toward reality after each
%    injection (iter 4: self=0.51 vs iter 3: self=0.64).
%
% 3. Limitations and future work:
%    - Hard negatives are accumulated from a fixed 200-problem pool — bank
%      saturates (~iter 12, hard_negs plateau near 97). Larger or dynamic
%      datasets would extend the benefit.
%    - Injection interval (every 3 iters) and ratio (50%) not ablated.
%    - Only one model (Qwen2.5-7B) and one domain (MATH L3-5) tested.
%    - The gap metric measures overconfidence, not downstream task performance.
%      Future work: measure collapse on open-ended generation where GT is harder
%      to define.

\paragraph{Why collapse happens.}
The mechanism is a closed reinforcement loop with a corrupted reward signal.
At iteration $t$, the model generates solutions, assigns self-scores, and
fine-tunes on the subset it believes to be correct.
By iteration 3--4, $\Delta_t$ already exceeds 0.20 in the Qwen baseline,
which means the model is systematically selecting \emph{wrong} completions
as positive training examples.
The LoRA adapter then reinforces the linguistic patterns that trigger
high self-scores---confident hedges, structurally plausible but incorrect
algebra, well-formatted answers to the wrong question---because those
patterns co-occur with the self-score signal, not with correctness.
The near-zero training loss by iteration 10 ($\mathcal{L}_{10} = 0.006$)
is the fingerprint of memorisation: the model has fitted its own
confabulations.
At that point $\gap$ continues to grow not because the self-verifier is
independently miscalibrating, but because $\bar{\gt}^\text{train}$ stays
flat while $\bar{\self}$ climbs on the back of those memorised completions.
The GT-anchored ablation closes the loop cleanly: swapping the training filter
from self-score to ground truth ($\gt > 0$) reverses the
trajectory---gap falls $0.190 \to 0.122$ over 20 iterations---because
the positive examples are now genuinely correct and the memorisation
incentive is removed.

\paragraph{Why three collapse regimes appear.}
The three models enter the loop with qualitatively different base competencies
on MATH L3--5, and this pre-training gap determines which regime they fall into.
Qwen2.5-7B begins with $\bar{\gt}^\text{train}_1 = 0.360$ (moderate accuracy);
it has enough genuine signal to sustain gradual, monotonic collapse over 20 iterations.
Mistral-7B-Instruct-v0.3 begins with $\bar{\gt}^\text{train}_1 = 0.045$---barely
above chance---so its self-scores are inflated from iteration 1 ($\gap_1 = 0.580$).
There is no recovery: the loop locks in on near-random completions immediately
and reinforces them, yielding the steepest final gap ($\gap_{20} = 0.850$).
Llama-3-8B occupies an intermediate regime: initial accuracy is low
($\bar{\gt}^\text{train}_1 = 0.230$) but not as catastrophic as Mistral's.
The gap peaks at iteration 13 ($\gap_{13} = 0.368$) and then partially recovers
to $\gap_{20} = 0.148$.
We attribute this non-monotonicity to LoRA distortion eventually degrading
the self-judgment mechanism itself: at high $\Delta_t$, the adapter has
overwritten enough of the instruction-following subspace that the model also
becomes less reliable at generating confident self-scores, suppressing $\bar{\self}$
alongside $\bar{\gt}^\text{train}$.
Crucially, this is \emph{not} genuine recalibration---$\bar{\gt}^\text{train}$
remains flat throughout the recovery phase---it is simply a second-order corruption
of the self-verifier.
The unifying principle across all three regimes is that the loop amplifies
whatever miscalibration exists at initialisation; models with lower base
competence enter the loop already miscalibrated and collapse fastest.

\paragraph{Why hard-negative injection helps.}
Hard negatives---examples where $\bar{\self} > \theta$ yet $\gt = 0$---are
precisely the failure cases that the default loop never trains on.
By injecting them into the fine-tuning batch every third iteration (using the
gold MATH solution as the training target), we give the model the only
ground-truth-anchored signal available without an external oracle.
The sawtooth pattern in Figure~\ref{fig:baseline-injection}(b) reflects this directly: gap
spikes at injection-step boundaries as the model is forced to contend with
its overconfident errors, then partially recovers between injections.
The steady-state gap of $0.170$ at iteration 20 represents a 47\% reduction
relative to the 0.320 baseline, and $\bar{\gt}^\text{val}$ trends upward to
0.50--0.52 versus flat in the baseline, confirming that genuine learning
accompanies the recalibration.

\paragraph{Limitations.}
Several constraints bound the scope of our conclusions.
First, hard negatives are drawn from a fixed 400-problem training pool;
the bank saturates as the model becomes uniformly overconfident on all
problems in the set (hard\_negs plateau near 62--67 in our runs).
Larger or dynamically augmented datasets would extend the benefit of injection
and are a direct avenue for future work.
Second, we ablate the injection interval ($N \in \{1,3,6\}$) and ratio
($R \in \{25\%, 50\%\}$) only after the supervisor review gate; those results
will inform optimal hyperparameter selection for the intervention.
Third, our evaluation domain is MATH L3--5 (and L5 only for the difficulty
ablation), a closed-form setting where ground truth is unambiguous.
Whether verification collapse occurs---and whether injection mitigates it---in
open-ended domains (instruction following, code generation, creative writing)
where $\gt$ is not directly computable remains an open question.
Finally, $\Delta_t$ measures overconfidence in self-verification, not
downstream task performance; a collapsed self-verifier is damaging primarily
insofar as it silently poisons the training signal.
Future work should instrument the relationship between $\Delta_t$ and
held-out performance on unseen benchmarks across more training iterations.

% ============================================================
\section{Conclusion}
\label{sec:conclusion}

% --- WRITING HOOK ---
%
% 4–5 sentences. Mirror the abstract.
%   1. Restate the problem.
%   2. Restate the finding (gap 0.10→0.32, 3×, gt_val flat).
%   3. Restate the mitigation (−47% with injection).
%   4. Broader implication: any self-improving system without external
%      grounding is at risk of verification collapse.
%   5. Call to action: measure the gap, not just the self-score.

Iterative self-improvement loops rest on the assumption that a model can
reliably distinguish its correct outputs from its incorrect ones.
We have shown that this assumption fails systematically: across three model
families and two dataset difficulties, the verification gap $\Delta_t$ grows
with training iterations, driven by a feedback loop in which the model
fine-tunes on its own overconfident errors.
The failure manifests in three capability-dependent regimes---gradual,
transient, and immediate collapse---but in all cases $\bar{\gt}^\text{val}$
remains flat while $\bar{\self}$ climbs, confirming that the apparent gains
are illusory.
A ground-truth-anchored training filter eliminates collapse entirely;
adversarial hard-negative injection reduces steady-state gap by 47\%
without any external oracle.
The practical implication is direct: any self-improving system should
instrument $\Delta_t$ explicitly.
Optimising self-score alone, without tracking the verification gap,
risks producing a model that is confidently wrong and growing more so with
every additional training iteration.

% ============================================================
\bibliographystyle{plainnat}
\bibliography{refs}

% ---- placeholder refs (move to refs.bib) ----
% @article{zelikman2022star,...}
% @article{yuan2024self,...}
% @article{deepseek2025azr,...}
% @article{hendrycks2021math,...}
% @article{qwen2025,...}
% @article{dao2023flashattention2,...}
% @article{gao2023scaling,...}  % RM overoptimization

\end{document}
